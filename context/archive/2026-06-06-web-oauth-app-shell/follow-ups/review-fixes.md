# Review fixes — web-oauth-app-shell (2026-06-06)

## Applied during triage

- **F1**: Plan addendum documenting `avatarUrl` cross-stack feature (`plan.md`).
- **F2**: Optimistic session clear on logout failure + try/finally navigation; new spec case (`auth.service.ts`, `app-shell.component.ts`, `auth.service.spec.ts`).

## Skipped

- **F3**: Guard initializer dependency comment — accepted implicit contract.
- **F4**: `isAuthenticatedStatus` structural check — deferred for MVP.
